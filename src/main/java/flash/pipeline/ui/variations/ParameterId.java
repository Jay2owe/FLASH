package flash.pipeline.ui.variations;

public enum ParameterId {
    THRESHOLD,
    MIN_SIZE,
    MAX_SIZE,

    PROB_THRESH,
    NMS_THRESH,
    LINKING_MAX,
    GAP_CLOSING_MAX,
    FRAME_GAP,
    AREA_MIN,
    AREA_MAX,
    QUALITY_MIN,
    INTENSITY_MIN,

    DIAMETER,
    FLOW_THRESHOLD,
    CELLPROB_THRESHOLD,
    MODEL
}
